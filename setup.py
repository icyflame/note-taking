from distutils.core import setup
import py2exe

setup(windows=['notes1NewApproach.py'])

#run "setup.py py2exe" in the command prompt after moving to this folder.

#then you can zip the folder dist and distribute it to others.
